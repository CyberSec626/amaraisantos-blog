
powershell

```powershell {2}
Add-DomainObjectAcl -TargetIdentity 
"dc=us,dc=techcorp,dc=local" -PrincipalIdentity studentuser1 -Rights All -PrincipalDomain us.techcorp.local -TargetDomain us.techcorp.local -Verbose
```


cmd

```cmd
ip a
```


bash

```bash
nc -nlvp 9090
```

sem linguagem

```
nc -nlvp 9090
```
